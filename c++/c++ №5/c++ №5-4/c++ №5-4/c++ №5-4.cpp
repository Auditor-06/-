#include <iostream>
using namespace std;

int main() {
    // Настройка локали для корректного отображения русского текста
    setlocale(LC_ALL, "Russian");

    int m, n;
    cout << "Введите размеры матрицы (строки и столбцы): ";
    cin >> m >> n;

    // Проверка на корректность размеров
    if (m <= 0 || n <= 0) {
        cout << "Ошибка: размеры должны быть положительными!" << endl;
        return 1;
    }

    // Ввод значения C
    int C;
    cout << "Введите значение C: ";
    cin >> C;

    // Динамическое выделение памяти для матрицы A
    int** A = new int* [m];
    for (int i = 0; i < m; i++) {
        A[i] = new int[n];
    }

    // Ввод элементов матрицы A
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            cout << "Введите элемент A[" << i << "][" << j << "]: ";
            cin >> A[i][j];
        }
    }

    // Динамическое выделение памяти для массива B (по одному элементу на строку)
    int* B = new int[m];

    // Обработка каждой строки матрицы A
    for (int i = 0; i < m; i++) {
        bool found = false; // флаг: найден ли элемент > C в текущей строке

        // Поиск первого элемента в строке, который больше C
        for (int j = 0; j < n; j++) {
            if (A[i][j] > C) {
                B[i] = A[i][j]; // записываем первый подходящий элемент в B
                found = true;
                break; // выходим из цикла, так как нужен только первый элемент
            }
        }

        // Если элемент не найден, записываем 0
        if (!found) {
            B[i] = 0;
        }
    }

    // Вывод матрицы A в виде таблицы
    cout << "\nМатрица A:" << endl;
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            cout << A[i][j] << "\t";
        }
        cout << endl;
    }

    // Вывод массива B
    cout << "\nМассив B (первые элементы строк > C, или 0, если таких нет):" << endl;
    for (int i = 0; i < m; i++) {
        cout << "B[" << i << "] = " << B[i] << endl;
    }

    // Освобождение памяти
    for (int i = 0; i < m; i++) {
        delete[] A[i];
    }
    delete[] A;
    delete[] B;

    system("pause"); // только для Windows
    return 0;
}
